services:
  - type: web
    name: ai-agent
    runtime: node
    region: singapore
    plan: free
    buildCommand: ./build.sh
    startCommand: bun start
    autoDeploy: true
    envVars:
      - key: OPENAI_API_KEY
        sync: false
