services:
  - type: web
    name: ai-agent
    runtime: node
    region: singapore
    plan: free
    buildCommand: bun install --production
    startCommand: bun start
    autoDeploy: true
    envVars:
      - key: OPENAI_API_KEY
        sync: false
